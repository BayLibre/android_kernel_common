{
  "comments": [
    {
      "robotId": "ayeaye",
      "robotRunId": "AR00000000000077436560",
      "url": "https://android-build.googleplex.com/builds/treetop/android-review/3079940?revision\u003d1",
      "key": {
        "uuid": "6ff82a1c_0a82fbc1",
        "filename": "/COMMIT_MSG",
        "patchSetId": 1
      },
      "lineNbr": 0,
      "author": {
        "id": 1757831
      },
      "writtenOn": "2024-05-10T08:02:38Z",
      "side": 1,
      "message": "Please **attach an open respin request bug** to this change. Refer the following link for more information:\nhttps://source.android.com/docs/core/architecture/kernel/gki-releases#respin\n\nPlease refer to the contribution guidelines for details:\nhttps://android.googlesource.com/kernel/common/+/refs/heads/android-mainline/README.md",
      "tag": "",
      "revId": "5dbd1f9c716abd821564a4fb2a4b420a73ca7bd2",
      "serverId": "85c56323-6fa9-3386-8a01-6480fb634889"
    },
    {
      "robotId": "ayeaye",
      "robotRunId": "AR00000000000077436560",
      "url": "https://android-build.googleplex.com/builds/treetop/android-review/3079940?revision\u003d1",
      "key": {
        "uuid": "672f6265_494db5b4",
        "filename": "/COMMIT_MSG",
        "patchSetId": 1
      },
      "lineNbr": 0,
      "author": {
        "id": 1757831
      },
      "writtenOn": "2024-05-10T08:02:38Z",
      "side": 1,
      "message": "Cherry pick is not clean. Checked against base branch android14-6.1.\nBase branch change: [3025590](https://android-review.googlesource.com/c/3025590).\n\n```\n      ## fs/userfaultfd.c ##\n     @@ fs/userfaultfd.c: static int userfaultfd_move(struct userfaultfd_ctx *ctx,\n      \t\treturn -EINVAL;\n      \n      \tif (mmget_not_zero(mm)) {\n    --\t\tmmap_read_lock(mm);\n    --\n    --\t\t/* Re-check after taking map_changing_lock */\n    --\t\tdown_read(\u0026ctx-\u003emap_changing_lock);\n    --\t\tif (likely(!atomic_read(\u0026ctx-\u003emmap_changing)))\n    --\t\t\tret \u003d move_pages(ctx, mm, uffdio_move.dst, uffdio_move.src,\n    --\t\t\t\t\t uffdio_move.len, uffdio_move.mode);\n    --\t\telse\n    --\t\t\tret \u003d -EAGAIN;\n    --\t\tup_read(\u0026ctx-\u003emap_changing_lock);\n    --\t\tmmap_read_unlock(mm);\n    ++\u003c\u003c\u003c\u003c\u003c\u003c\u003c HEAD   (9ce9b9 BACKPORT: mm: add vma_assert_locked() for !CONFIG_PER_VMA_LO)\n    + \t\tmmap_read_lock(mm);\n    + \n    + \t\t/* Re-check after taking mmap_lock */\n    +@@ fs/userfaultfd.c: static int userfaultfd_move(struct userfaultfd_ctx *ctx,\n    + \t\t\tret \u003d -EINVAL;\n    + \n    + \t\tmmap_read_unlock(mm);\n    ++\u003d\u003d\u003d\u003d\u003d\u003d\u003d\n     +\t\tret \u003d move_pages(ctx, uffdio_move.dst, uffdio_move.src,\n     +\t\t\t\t uffdio_move.len, uffdio_move.mode);\n    ++\u003e\u003e\u003e\u003e\u003e\u003e\u003e CHANGE (ce2896 BACKPORT: userfaultfd: use per-vma locks in userfaultfd oper)\n      \t\tmmput(mm);\n      \t} else {\n      \t\treturn -ESRCH;\n    @@ mm/userfaultfd.c: struct vm_area_struct *find_dst_vma(struct mm_struct *dst_mm,\n     +\tdst_vma \u003d lock_vma(dst_mm, dst_start);\n     +\tif (IS_ERR(dst_vma) || validate_dst_vma(dst_vma, dst_start + len))\n     +\t\treturn dst_vma;\n    - \n    ++\n     +\tvma_end_read(dst_vma);\n     +\treturn ERR_PTR(-ENOENT);\n     +}\n    @@ mm/userfaultfd.c: struct vm_area_struct *find_dst_vma(struct mm_struct *dst_mm,\n     +{\n     +\tvma_end_read(vma);\n     +}\n    -+\n    + \n     +#else\n     +\n     +static struct vm_area_struct *uffd_mfill_lock(struct mm_struct *dst_mm,\n    @@ mm/userfaultfd.c: static pmd_t *mm_alloc_pmd(struct mm_struct *mm, unsigned long\n     + * called with either vma-lock or mmap_lock held, it will release the lock\n     + * before returning.\n       */\n    - static __always_inline ssize_t __mcopy_atomic_hugetlb(\n    - \t\t\t\t\t      struct userfaultfd_ctx *ctx,\n    -@@ mm/userfaultfd.c: static __always_inline ssize_t __mcopy_atomic_hugetlb(\n    - \t\t\t\t\t      bool wp_copy)\n    + static __always_inline ssize_t __mcopy_atomic_hugetlb(struct mm_struct *dst_mm,\n    + \t\t\t\t\t      struct vm_area_struct *dst_vma,\n    +@@ mm/userfaultfd.c: static __always_inline ssize_t __mcopy_atomic_hugetlb(struct mm_struct *dst_mm,\n    + \t\t\t\t\t      bool wp_copy,\n    + \t\t\t\t\t      atomic_t *mmap_changing)\n      {\n    - \tstruct mm_struct *dst_mm \u003d dst_vma-\u003evm_mm;\n    --\tint vm_shared \u003d dst_vma-\u003evm_flags \u0026 VM_SHARED;\n    ++\u003c\u003c\u003c\u003c\u003c\u003c\u003c HEAD   (9ce9b9 BACKPORT: mm: add vma_assert_locked() for !CONFIG_PER_VMA_LO)\n    + \tint vm_shared \u003d dst_vma-\u003evm_flags \u0026 VM_SHARED;\n    ++\u003d\u003d\u003d\u003d\u003d\u003d\u003d\n    ++\tstruct mm_struct *dst_mm \u003d dst_vma-\u003evm_mm;\n    ++\u003e\u003e\u003e\u003e\u003e\u003e\u003e CHANGE (ce2896 BACKPORT: userfaultfd: use per-vma locks in userfaultfd oper)\n      \tssize_t err;\n      \tpte_t *dst_pte;\n      \tunsigned long src_addr, dst_addr;\n    -@@ mm/userfaultfd.c: static __always_inline ssize_t __mcopy_atomic_hugetlb(\n    +@@ mm/userfaultfd.c: static __always_inline ssize_t __mcopy_atomic_hugetlb(struct mm_struct *dst_mm,\n    + \t * feature is not supported.\n      \t */\n      \tif (mode \u003d\u003d MCOPY_ATOMIC_ZEROPAGE) {\n    - \t\tup_read(\u0026ctx-\u003emap_changing_lock);\n    --\t\tmmap_read_unlock(dst_mm);\n    ++\u003c\u003c\u003c\u003c\u003c\u003c\u003c HEAD   (9ce9b9 BACKPORT: mm: add vma_assert_locked() for !CONFIG_PER_VMA_LO)\n    + \t\tmmap_read_unlock(dst_mm);\n    ++\u003d\u003d\u003d\u003d\u003d\u003d\u003d\n    ++\t\tup_read(\u0026ctx-\u003emap_changing_lock);\n     +\t\tuffd_mfill_unlock(dst_vma);\n    ++\u003e\u003e\u003e\u003e\u003e\u003e\u003e CHANGE (ce2896 BACKPORT: userfaultfd: use per-vma locks in userfaultfd oper)\n      \t\treturn -EINVAL;\n      \t}\n      \n    @@ mm/userfaultfd.c: retry:\n      \t\terr \u003d -EINVAL;\n      \t\tif (vma_hpagesize !\u003d vma_kernel_pagesize(dst_vma))\n     -\t\t\tgoto out_unlock;\n    --\n    --\t\tvm_shared \u003d dst_vma-\u003evm_flags \u0026 VM_SHARED;\n    --\t}\n     +\t\t\tgoto out_unlock_vma;\n      \n    +-\t\tvm_shared \u003d dst_vma-\u003evm_flags \u0026 VM_SHARED;\n    +-\t}\n    +-\n     -\t/*\n     -\t * If not shared, ensure the dst_vma has a anon_vma.\n     -\t */\n    @@ mm/userfaultfd.c: retry:\n      \t}\n      \n     @@ mm/userfaultfd.c: retry:\n    + \t\tcond_resched();\n      \n      \t\tif (unlikely(err \u003d\u003d -ENOENT)) {\n    - \t\t\tup_read(\u0026ctx-\u003emap_changing_lock);\n    --\t\t\tmmap_read_unlock(dst_mm);\n    ++\u003c\u003c\u003c\u003c\u003c\u003c\u003c HEAD   (9ce9b9 BACKPORT: mm: add vma_assert_locked() for !CONFIG_PER_VMA_LO)\n    + \t\t\tmmap_read_unlock(dst_mm);\n    ++\u003d\u003d\u003d\u003d\u003d\u003d\u003d\n    ++\t\t\tup_read(\u0026ctx-\u003emap_changing_lock);\n     +\t\t\tuffd_mfill_unlock(dst_vma);\n    ++\u003e\u003e\u003e\u003e\u003e\u003e\u003e CHANGE (ce2896 BACKPORT: userfaultfd: use per-vma locks in userfaultfd oper)\n      \t\t\tBUG_ON(!page);\n      \n      \t\t\terr \u003d copy_huge_page_from_user(page,\n    @@ mm/userfaultfd.c: retry:\n      \t\t\t\terr \u003d -EFAULT;\n      \t\t\t\tgoto out;\n      \t\t\t}\n    --\t\t\tmmap_read_lock(dst_mm);\n    --\t\t\tdown_read(\u0026ctx-\u003emap_changing_lock);\n    --\t\t\t/*\n    --\t\t\t * If memory mappings are changing because of non-cooperative\n    --\t\t\t * operation (e.g. mremap) running in parallel, bail out and\n    --\t\t\t * request the user to retry later\n    --\t\t\t */\n    --\t\t\tif (atomic_read(\u0026ctx-\u003emmap_changing)) {\n    --\t\t\t\terr \u003d -EAGAIN;\n    --\t\t\t\tbreak;\n    --\t\t\t}\n    ++\u003c\u003c\u003c\u003c\u003c\u003c\u003c HEAD   (9ce9b9 BACKPORT: mm: add vma_assert_locked() for !CONFIG_PER_VMA_LO)\n    + \t\t\tmmap_read_lock(dst_mm);\n    + \t\t\t/*\n    + \t\t\t * If memory mappings are changing because of non-cooperative\n    +@@ mm/userfaultfd.c: retry:\n    + \t\t\t\terr \u003d -EAGAIN;\n    + \t\t\t\tbreak;\n    + \t\t\t}\n    ++\u003d\u003d\u003d\u003d\u003d\u003d\u003d\n    ++\u003e\u003e\u003e\u003e\u003e\u003e\u003e CHANGE (ce2896 BACKPORT: userfaultfd: use per-vma locks in userfaultfd oper)\n      \n      \t\t\tdst_vma \u003d NULL;\n      \t\t\tgoto retry;\n     @@ mm/userfaultfd.c: retry:\n    + \t}\n      \n      out_unlock:\n    - \tup_read(\u0026ctx-\u003emap_changing_lock);\n    --\tmmap_read_unlock(dst_mm);\n    ++\u003c\u003c\u003c\u003c\u003c\u003c\u003c HEAD   (9ce9b9 BACKPORT: mm: add vma_assert_locked() for !CONFIG_PER_VMA_LO)\n    + \tmmap_read_unlock(dst_mm);\n    ++\u003d\u003d\u003d\u003d\u003d\u003d\u003d\n    ++\tup_read(\u0026ctx-\u003emap_changing_lock);\n     +out_unlock_vma:\n     +\tuffd_mfill_unlock(dst_vma);\n    ++\u003e\u003e\u003e\u003e\u003e\u003e\u003e CHANGE (ce2896 BACKPORT: userfaultfd: use per-vma locks in userfaultfd oper)\n      out:\n      \tif (page)\n      \t\tput_page(page);\n    -@@ mm/userfaultfd.c: static __always_inline ssize_t __mcopy_atomic(struct userfaultfd_ctx *ctx,\n    +@@ mm/userfaultfd.c: static __always_inline ssize_t __mcopy_atomic(struct mm_struct *dst_mm,\n      \tcopied \u003d 0;\n      \tpage \u003d NULL;\n      retry:\n    @@ mm/userfaultfd.c: static __always_inline ssize_t __mcopy_atomic(struct userfault\n      \t/*\n      \t * If memory mappings are changing because of non-cooperative\n     @@ mm/userfaultfd.c: retry:\n    - \tif (atomic_read(\u0026ctx-\u003emmap_changing))\n    + \tif (mmap_changing \u0026\u0026 atomic_read(mmap_changing))\n      \t\tgoto out_unlock;\n      \n     -\t/*\n    @@ mm/userfaultfd.c: retry:\n      \t\tpmd_t dst_pmdval;\n      \n     @@ mm/userfaultfd.c: retry:\n    + \t\tif (unlikely(err \u003d\u003d -ENOENT)) {\n      \t\t\tvoid *page_kaddr;\n      \n    - \t\t\tup_read(\u0026ctx-\u003emap_changing_lock);\n    --\t\t\tmmap_read_unlock(dst_mm);\n    ++\u003c\u003c\u003c\u003c\u003c\u003c\u003c HEAD   (9ce9b9 BACKPORT: mm: add vma_assert_locked() for !CONFIG_PER_VMA_LO)\n    + \t\t\tmmap_read_unlock(dst_mm);\n    ++\u003d\u003d\u003d\u003d\u003d\u003d\u003d\n    ++\t\t\tup_read(\u0026ctx-\u003emap_changing_lock);\n     +\t\t\tuffd_mfill_unlock(dst_vma);\n    ++\u003e\u003e\u003e\u003e\u003e\u003e\u003e CHANGE (ce2896 BACKPORT: userfaultfd: use per-vma locks in userfaultfd oper)\n      \t\t\tBUG_ON(!page);\n      \n      \t\t\tpage_kaddr \u003d kmap_local_page(page);\n     @@ mm/userfaultfd.c: retry:\n    + \t}\n      \n      out_unlock:\n    - \tup_read(\u0026ctx-\u003emap_changing_lock);\n    --\tmmap_read_unlock(dst_mm);\n    ++\u003c\u003c\u003c\u003c\u003c\u003c\u003c HEAD   (9ce9b9 BACKPORT: mm: add vma_assert_locked() for !CONFIG_PER_VMA_LO)\n    + \tmmap_read_unlock(dst_mm);\n    ++\u003d\u003d\u003d\u003d\u003d\u003d\u003d\n    ++\tup_read(\u0026ctx-\u003emap_changing_lock);\n     +\tuffd_mfill_unlock(dst_vma);\n    ++\u003e\u003e\u003e\u003e\u003e\u003e\u003e CHANGE (ce2896 BACKPORT: userfaultfd: use per-vma locks in userfaultfd oper)\n      out:\n      \tif (page)\n      \t\tput_page(page);\n    -@@ mm/userfaultfd.c: int mwriteprotect_range(struct userfaultfd_ctx *ctx, unsigned long start,\n    +@@ mm/userfaultfd.c: int mwriteprotect_range(struct mm_struct *dst_mm, unsigned long start,\n      \t\tgoto out_unlock;\n      \n      \terr \u003d -ENOENT;\n    @@ mm/userfaultfd.c: static int validate_move_areas(struct userfaultfd_ctx *ctx,\n     +\tif (err)\n     +\t\tmmap_read_unlock(mm);\n     +\treturn err;\n    - }\n    - \n    ++}\n    ++\n     +static void uffd_move_unlock(struct vm_area_struct *dst_vma,\n     +\t\t\t     struct vm_area_struct *src_vma)\n     +{\n     +\tmmap_assert_locked(src_vma-\u003evm_mm);\n     +\tmmap_read_unlock(dst_vma-\u003evm_mm);\n    -+}\n    + }\n     +#endif\n    -+\n    + \n      /**\n       * move_pages - move arbitrary anonymous pages of an existing vma\n       * @ctx: pointer to the userfaultfd context\n```\n\n\nPlease refer to the contribution guidelines for details:\nhttps://android.googlesource.com/kernel/common/+/refs/heads/android-mainline/README.md",
      "tag": "",
      "revId": "5dbd1f9c716abd821564a4fb2a4b420a73ca7bd2",
      "serverId": "85c56323-6fa9-3386-8a01-6480fb634889"
    }
  ]
}